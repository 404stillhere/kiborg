---
model: claude-opus-4-8
effort: high
tags: [kiborg, мозг, mind, council, совет, wiring, впайка, ход-Г]
date: 2026-07-13
---

# Handoff: kiborg — совет ВПАЯН в живой цикл (ход Г, выкачено)

**Возобновить:** файл этот; модель **opus-4.8**, effort **high**, если трогать архитектуру мозга.
Мелкая доводка/тесты — тянет sonnet/standard-coder.
**Читать первым:** `.brain/changelog.md` (низ) + `.brain/design/mozg-plan.md` + `.brain/design/mind-council.md`.
Предыдущие хендофы: `handoff_2026-07-13_mozg.md` (что делать с мозгом), `handoff_2026-07-13_mind-council.md` (парал. сессия построила совет).

## Что сделано этой сессией (выкачено, коммит 6de12a4, push origin/master)
Юзер снял гейт («да») → **впаял взвешенный совет на шаг отбора идей** (ход Г из mozg-plan).
- `cyborg/wiring.py`: `_run_rank` идёт через `mind.think` (арбитр rank_ideas 0.41 + интуиция
  ask_llm 0.39; оркестр 0.20 спит за флагом). Совет ставит балл каждой идее → топ-3 по баллу.
  Форма `ideas_best` цела, downstream (scrub/deliver) не тронут. Сбой/деградация → тихий откат
  на прежний rank_ideas (конвейер не встаёт; офлайн, без ключей — байт-в-байт как раньше).
- `cyborg/run.py`: провод `keychain.build_chain()` → `env['llm_chain']` (оживляет интуицию).
- `cyborg/tests/test_wiring.py`: +8 тестов на путь совета. **189 зелёных** (darbot venv).
- **Ядро brain.py/core.py и файлы парал. сессии mind.py/advisors.py/keychain.py — НЕ тронуты.**
- Живьём проверено: совет включается, 2 голоса (rank_ideas·0.51 + ask_llm·0.49), реальный
  closerouter, топ-3 лучше (выкинул пустышку, которую одиночный судья держал).

## Обход хардкода (важно для парал. сессии)
`AskLlmAdvisor._ask` жёстко ставил `max_tokens=256` → рассуждающие модели closerouter тратили
весь лимит на скрытое рассуждение и возвращали пусто → интуиция молчала. Юзер: «убрать вообще».
Решено на СВОЕЙ стороне: `wiring._IntuitionNoCap` (подкласс без max_tokens в payload), их
`advisors.py` цел. **Долг:** когда парал. сессия сделает `max_tokens` ручкой в context —
`_IntuitionNoCap` удалить (сейчас это обход чужого хардкода, копия их `_ask` минус одна строка).

## Открытые решения юзера (ждут)
1. **Развилка имён:** «мозг как органы» (органы = «Мозг·…») vs «мозг = планировщик». НЕ выбрано,
   впайке не мешала.
2. **Будить ли оркестр** (7-модельный совет, вес 0.20, дорогой): сейчас за флагом
   `KIBORG_WAKE_ORCHESTRA`, спит. Отдельный гейт — включать словом.
3. **Главная работа киборга** — от неё зависит, нужны ли развилки/планировщик-LLM вообще.

## Осторожно (грабли)
- Тесты гонять **darbot venv**: `M:/projects/darbot/venv/Scripts/python.exe run_tests.py` из корня
  (funpay venv без pytest врёт «passed=0, всё зелено»).
- Пульт (`panel/serve.py`) зовёт `run.py` подпроцессом — «приноси идеи» теперь идёт через совет,
  делает 1 сетевой вызов closerouter на шаг отбора (интуиция). Медленнее прежнего на пару секунд.
- closerouter даёт перемежающийся 502 по отдельным моделям — на то и цепочка из 4, терпеть.
- Мозг-ПЛАНИРОВЩИК (brain.py) по-прежнему на stub. Совет впаян на ОТБОР идей, НЕ на выбор шагов.
- Мусор `panel/index.html.bak-*033206` (untracked, не в git) — можно удалить, я не трогал.

## Следующий шаг
Дождаться выбора юзера по 3 пунктам выше. Быстрый выигрыш к автономности — разбудить оркестр
(флаг) и глянуть на живом пульте полный совет из трёх голосов. Реализация — свериться с парал.
сессией (keychain.py), чтобы не столкнуться.
