---
project: kiborg
path: M:/projects/kiborg
date: 2026-07-14
topic: ebash-автоном — ре-оценка читаемости + выгреб всего свободного (покрытие/доки/гигиена/интеграция)
status: свободное автономное ИСЧЕРПАНО; всё дальнейшее гейтнуто на юзера
model: claude-sonnet-5     # остаток — не код, а решения юзера; opus не нужен
effort: medium
tags: [kiborg, ebash, coverage, docs, integration, gated]
---

# Handoff: ebash-сессия покрытия/доков (kiborg)

Продолжение из `handoff_2026-07-13_council-in-fon.md`. Всё ВЫКАЧЕНО (GitHub 404stillhere/kiborg,
master) и зелёное. Читать НЕ обязательно — свободная работа закрыта; файл на случай возврата.

## Что сделано (17 коммитов за сессию, все запушены)
1. **Ре-оценка переписанного в readability_gate** (dc65145+74be1ef): правку берём ТОЛЬКО если
   новый why строго лучше старого В ПАРЕ (скептик поймал: батчевый балл vs сольный несравнимы).
2. **Модернизация** (8dc3a32): убрал 2 мёртвых импорта (ruff F401). vulture — чисто (остальное ложное/заморожено).
3. **Покрытие ВСЕХ модулей** (тесты 197→239): дыры источников — HN happy-path/per_n/telegram payload
   (031a7b4); `finish_step` был без теста +9 (87fe027); `executor` прод-гейт +11 (18e2256);
   `observe_sources` +4 (603a4d3); конфиг автономности `_load_auto/_save_auto` +6 (8655440).
4. **Интеграция РЕАЛЬНОЙ цепочки** (8a7fe25+dbedd33): `build_organs` и `build_harvest_organs` —
   стыковка produces↔consumes сквозняком (инбокс-путь →deliver и автономный →stash), + динамика
   на стабах. Раньше прогонщик тестился на фикстуре, стадии — юнитами; сквозной цепи не было.
5. **Доки** — корневой README (c040ad9), panel/README (e90a51d), дрейф idea_engine/README (115671c).
6. **Git-гигиена** — 34 .bak из трекинга (bc2ab46), `*.log` в .gitignore (83a9dfd). Локальные целы.

Батарея-2 прогнана командами: jscpd дубли=0, сироты=0, запутки=0. b-1: git чист, 0 маркеров.

## Состояние среды
- Пульт: `M:/projects/funpay/venv/Scripts/python.exe M:/projects/kiborg/panel/serve.py` → :8737. Автономность ON, 60 мин.
- Тесты: `M:/projects/darbot/venv/Scripts/python.exe run_tests.py` из корня (голый pytest врёт).
- Заморожено: `mind/advisors/keychain/llm_keys.env` (секреты), `brain.py`/`core.py`, `collect_source.py`+роутер (гейт).

## ОТКРЫТОГО СВОБОДНОГО НЕТ. Рычаги — за юзером (по убыванию ценности):
1. **Разобрать пакет фабрики б-3** (`.feature-lab/router.json`, feat-1/feat-2 reviewed=false) → снимет
   замок петли → фабрика построит НОВЫЙ пакет идей (это и есть «автономно искать идеи»). Мой разбор
   в `.brain/loose-ends.md`: **feat-2 НЕ мёржить** (ломает дорожку B), **feat-1 суперседнута**.
2. **feat-1 вскрыла РЕАЛЬНЫЙ баг (правка в замороженном core.py):** при лежащей сети collect_source
   отдаёт 4 захардкоженных `_FALLBACK`-заголовка с degraded=True; cyborg-путь доставляет их как
   свежие HN КАЖДЫЙ цикл, сигнала нет (`note['warning']` write-only). Юзер месяц получал бы статичный
   сэмпл под видом работы. Чинить у источника/классификатором — гейт юзера (ядро).
3. panel-стиль (облик рыцаря — 3 стиля готовы, выбрать финальный), ТГ-доставка (нужен бот-токен), LLM-мозг.

## Уроки в глобальную память
- `feedback-deploy-secret-scan-broken` — инлайн `grep && echo СТОП || … && push` НЕ гейтит; скан отдельным шагом по exit-коду.
- `feedback-ostanovochka-typo-ebash-hook` — опечатка в стоп-слове не снимает ebash-хук (точная сверка).
