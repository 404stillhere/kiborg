---
model: claude-opus-4-8
effort: high
tags: [kiborg, мозг, отделы, оркестр, council_woken, пульт, видишь-воду, автономность]
date: 2026-07-13
---

# Handoff: kiborg — мозг как отделы + оркестр по умолчанию + видимость совета

**Возобновить:** этот файл; модель **opus-4.8**, effort **high** если трогать архитектуру мозга/автономию.
Мелочи по пульту/тестам — тянет sonnet.
**Читать первым:** `.brain/changelog.md` (верх, свежая запись) + путь чата
`.brain/paths/2026-07-13_brain-departments.md`.

---

## ⭐ ОБНОВЛЕНИЕ (тот же чат, после /compact) — деплой e49560c
Главное этого куска: **починил генератор идей — он всё время был на ЗАГЛУШКЕ.**

1. **Генератор сведён с интуицией на ОДИН провайдер/ключ.** Юзер увидел в инбоксе мусор
   («Идея по мотиву: <заголовок>», brain:stub) → корень: `cyborg/ask_llm.py` был отдельным
   проводом к **Gemini** (ключ gemini.md), а Gemini на этой сети **недоступен** (SSL handshake
   timeout, как reddit/gh_trending). `ideate` молча падал на stub → идеи = пересказ заголовков.
   Фикс (юзер: «ask_llm должен быть дипсик»): переписал `ask_llm.py` на цепочку интуиции из
   `keychain` (closerouter deepseek→glm→muse→codex, один ключ CLOSEROUTER_API_KEY) через
   DarBench `organ.js`. Gemini-провод убран. Живьём: ideate даёт **brain:llm** (реальные идеи).
2. **Оркестр голосует ВСЕГДА** (юзер хотел так изначально). Отбор через `mind.deliberate`
   (все советники безусловно), не `mind.think` (там оркестр просыпался лишь при сомнении
   интуиции — эвристика разброса баллов, кривая: зовёт на ленивых баллах, молчит на уверенно-
   неверных). Правка в `wiring._rank_by_council`. Рецензенты — параллельно (max_workers).
3. **Пульт: таймаут 180→600с.** Always-on оркестр = ~3 мин/прогон (судит 6 идей по очереди —
   этот цикл в чужом `advisors.py`, не трогал). Раньше пульт убивал прогон на 180с. Отдел
   «Совет» на пульте → «включён · всегда».
4. **⚠️ mind.py/advisors.py/keychain.py/llm_keys.env — НЕ тронуты** (параллельная сессия). Всё
   делал в своих: ask_llm.py/wiring.py/run.py/harvest.py/serve.py/panel + тесты. 191 зелёных.
5. **Gemini** остался лишь как ОДИН рецензент совета (ключ GEMINI_API_KEY в llm_keys.env, свой
   провод) — сейчас тоже недоступен, но совет многопровайдерный, молча выживает. `gemini.md`
   генератору больше не нужен (не удалял — секрет).

**Осторожно (новое):** прогон теперь ~3 мин, не мгновенный — это норма (оркестр). closerouter
даёт перемежающийся 502 — на то цепочка. Пульт перезапущен под funpay venv на :8737 (живой).

**Следующий шаг (обновлён):** киборг наконец выдаёт настоящие идеи. Дальше по желанию юзера —
качество идей / автономный крон (осторожно: каждый прогон ~3 мин × N) / вопрос «сигнал лучше»
(ниже, для саморазвития). Ничего не начато — ждёт юзера.

---
_Ниже — исходный handoff (départements + оркестр по умолчанию + видимость), уже выкачено раньше._

## Что сделано этой сессией (всё выкачено в origin/master)
Три правки, три пуша. Ядро `brain.py`/`core.py` и чужие `mind.py`/`advisors.py`/`keychain.py` — НЕ тронуты.

1. **cf1e4cc — Мозг как отделы (пульт).** Карточка «🧠 Мозг — отделы» на пульте: 5 отделов
   (планировщик/генератор/судья/интуиция/совет) с честным состоянием (🟢 живой / ⚪ заглушка /
   🟡 спит). Совет убран из «🌱 куда растёт» — он больше не будущее, впаян. **Это закрыло развилку
   имён:** «мозг=планировщик vs думающие органы» — ложная, мозг = несколько отделов (ход юзера
   «есть же разные отделы»). Файл: `panel/index.html` (renderBrain).

2. **cf1e4cc — Оркестр включён по умолчанию.** `run.py`: флаг перевёрнут. Было opt-in
   (`KIBORG_WAKE_ORCHESTRA`), стало ON по умолчанию; заглушить = `KIBORG_SLEEP_ORCHESTRA=1`.
   Юзер: «умный сомневается → зовёт совет». Оркестр доступен всегда, будится ТОЛЬКО при сомнении
   интуиции (escalate_gap), не на каждом шаге — иначе 7 вызовов зря.

3. **333ac1c — Видно, когда оркестр просыпается.** Провёл `council_woken` от совета до глаз:
   `orchestrator.py` (+1 аддитивный ключ `mem.data['council']`) → `run.py` пишет «оркестр
   ПРОСНУЛСЯ/спал · голоса: …» в живую консоль и в `runs.md` → `serve.py` парсит хвост в поле
   `.council` → пульт рисует бейдж 🏛 в журнале (проснулся — жёлтым).
   **Живьём подтверждено:** прогон дал «оркестр ПРОСНУЛСЯ · голоса: rank_ideas+ask_llm+orchestra» —
   7-модельный совет реально включается. **189 тестов зелёные** весь путь.

## ⚠️ Урок сессии: «видишь воду?» — нырнули в мираж
Юзер сказал «главная работа = автономная работа без нас», «хочу ИИ который сам развивается».
Я на каждой его фразе раздувал архитектуру (планировщик, пульс, лестница саморазвития, «сигнал
правды») — ноль кода, всё в воздухе. Юзер оборвал: «видишь воду? верни меня». **Корень был: бросили
чинить киборга, начали проектировать мечту.** Вернулись на твёрдую землю — маленькую честную правку
пульта. Осторожно в след. сессии: НЕ проектировать автономию/саморазвитие в воздухе.

## Открытые решения юзера (ЗАПАРКОВАНЫ, не решать за него)
1. **Главная работа = автономность + саморазвитие** — заявлена, но НЕ проектируется (сознательно).
2. **Ключевой нерешённый вопрос:** чтобы «сам развивался», нужен **сигнал «стало лучше»**. Откуда?
   От юзера (спорит с автономией) / от мира (нужен канал) / сам себе (опасно — «сам себя хвалит»).
   Без этого сигнала любой саморост = имитация. Это ГЛАВНЫЙ блокер, если браться за автономию.
3. **Планировщик** (`brain.py`) по-прежнему stub; «мозг-планировщик на LLM» — в «куда растёт».

## Быстрые честные шаги (маленькие, приземлённые)
- Инбокс полон 3/3 (#17/#18/#19, judged=council) — новые идеи не придут, пока не разберёшь.
- Фабрика фич под замком: 2 фичи ждут ревью (`.feature-lab`).

## Осторожно (грабли)
- Тесты гонять **darbot venv**: `M:/projects/darbot/venv/Scripts/python.exe run_tests.py` из корня
  (funpay venv без pytest врёт «зелено»). Пульт запускать любым python3 (стдлиб).
- Пульт зовёт `run.py` подпроцессом; правку `serve.py` подхватит только ПЕРЕЗАПУСК сервера.
- Оркестр теперь ON: когда просыпается — до 7 вызовов моделей за шаг. На автопрогонах капает.
- closerouter даёт перемежающийся 502 — на то и цепочка из 4, терпеть.
- Мусор `panel/index.html.bak-*` (untracked) — можно удалить, не трогал.

## Следующий шаг
Если юзер даст «сигнал лучше/хуже» (п.2) — тогда автономия/саморазвитие обретают почву, можно
проектировать. Без него — держаться приземлённых правок пульта/органов, не грести в мираж.
