---
model: claude-opus-4-8 (или sonnet для рутины harvest)
effort: medium
tags: [kiborg, ebash, panel, рыцарь, оживление, harvest, source-root, coverage, jscpd]
date: 2026-07-12
---

# Kiborg — сессия «Ебашь»: пульт ожил + корень узкого источника решён

Проект: **M:/projects/kiborg**. Запущено словом «Ебашь» (автономный цикл ebash, ~6ч).
**Ядро cyborg/idea_engine НЕ тронуто** (заморозка юзера держится) — вся работа в `panel/` и
новых файлах accumulator. Цикл СНЯТ юзером на остановочке (будильник погашен, cron пуст).

## Что сделано (по фазам)

**Фаза 1 — правки в моей незамороженной зоне (11 артефактов):**
1. Пульт ОЖИЛ (`panel/index.html`): сердце-ромб бьётся (темп от заполненности инбокса 1.6с→0.7с),
   тело дышит в простое (замирает при прогоне), нервы светятся при прогоне. +prefers-reduced-motion.
2. Блок «🌱 куда растёт»: 5 ghost-силуэтов будущих органов на РЕАЛЬНЫХ карточках каталога
   (collect_tg_news, search_web, ask_llm, generate_image_prompts, review_content).
3. У каждого органа «пример вывода» (реальный из живого состояния + иллюстративный где нет).
4. `cyborg/README.md` выправлен: «3 органа»→7, рабочая фраза-пример, словоформенный лимит роутера.
5. serve.py захарден: атомарная запись layout + потолок ключей + bool-guard; +9 тестов (было 0).
6. Убран мёртвый маршрут `/preview.html` (500→404).
7. ⭐ КОРЕНЬ узкого источника: `SOURCE_N=30` в harvest.py (collect_source читает n из env ПО ДИЗАЙНУ)
   — расширил БЕЗ правки ядра. Копилка пробила застревание на 6.
8. Кнопка «🌾 Собрать в копилку» в пульте (harvest из UI).
9. `--force` для кнопки (ручной клик перебивает гейт «лента не менялась») + убран лишний fetch.
10. watchdog-таймаут подпроцесса прогона (180с — пульт не залипает).
11. Клон карточек идей (ideaCard↔renderStash) вынесен в `ideaBody()`.

**Фаза 2 — домер приборами (по требованию stop-хука):**
- Coverage (stdlib `trace`, coverage не установлен): вскрыл дыры `stash.py` — загрузка битого .jsonl,
  Jaccard-дедуп ПОХОЖИХ (тесты били только точный повтор) → закрыл, класс Stash **41/41**.
- jscpd: 1 клон (harvest._log ↔ run.py _log_run) — обоснованно ОСТАВЛЕН (дедуп невозможен без правки
  замороженного run.py); сирот в зоне нет.
- Регрессия основного пути пульта («▶ Принеси идеи») — зелёная (доставил 3 в инбокс, карточки/кнопки ок).
- Итог: **91 тест** (cyborg 50 · idea_engine 32 · panel 9).

**Фаза 3 — работа петли (harvest):**
- Каждые ~30 мин harvest. Такт **1800с** подтверждён: HN топ-30 копит смену за ~30-60 мин, гейт
  корректно скипает неизменную ленту (без траты Gemini). Копилка **6→35 живых идей** (все llm, дедуп держит).

## ⚡ ГЛАВНЫЙ УРОК СЕССИИ
Допущение «шире источник = правка сбора = ядро = гейт юзера» было **ЛОЖНЫМ**. collect_source читает
лимит `n` из env по дизайну → расширение источника = КОНФИГ (мой файл harvest.py), НЕ правка ядра.
Проверяй, действительно ли «нужен юзер», ДО того как объявить работу гейтнутой.

## Открытые ГЕЙТЫ на юзера (не трогал — личное/внешнее/co-create)
- **Облик рыцаря** (цвет/настроение/глаза-камеры) — ЛИЧНОЕ, со-создаётся с юзером, не доносить готовым.
- **Копилка 35 идей** — разобрать; ранжирование «лучшее сверху» — co-create-решение (НЕ строить сам).
- ТГ-доставка (токен бота); cron-автоцикл (жёсткий гейт); LLM-мозг (future); разбор feat-1/2 фабрики (loop-lock).
- Шире ДРУГИМИ сайтами-источниками (github-trending и т.п.) — это уже правка collect_source = ядро.

## Ключевые файлы
- Пульт: `panel/index.html` (весь UI, читается на лету), `panel/serve.py` (рестарт при правке серверной части),
  `panel/bodies.js`, `panel/tests/test_serve.py`.
- Накопитель: `cyborg/harvest.py` (SOURCE_N=30, --force), `cyborg/stash.py`, `cyborg/stash_sink.py`.
- Данные: `cyborg/data/idea_stash.md` (копилка человеку, 35 идей), `cyborg/data/runs.md` (журнал).
- Знания: `.brain/changelog.md`, `.brain/loose-ends.md`, `.brain/ebash-queue.md` (журнал цикла).

## NB для следующего чата
- Запуск пульта: `python M:/projects/kiborg/panel/serve.py` → http://127.0.0.1:8737 (или preview `kiborg-panel`).
- serve.py/bodies.js требуют РЕСТАРТА сервера (python не хот-релоадит); index.html/layout — на лету.
- git у проекта НЕТ → откат через .bak с датой-временем (их за сессию накопилось много в panel/ и cyborg/).
- Хук ebash всю сессию блокил park кандидатом DarNet — кросс-проектный шум (сессия привязана к kiborg).
