# секреты — НИКОГДА не коммитить
gemini.md
llm_keys.env
*.key
.env

# данные/состояние — перегенерируются
cyborg/data/
idea_engine/data/
**/finish_cursor.json
panel/auto.json
*.log

# бэкапы-снапшоты редактора (панель) — не в git
*.bak-*

# python
__pycache__/
*.pyc

# песочница ночной фабрики фич
.feature-lab/

# приватная память проекта — никогда не коммитить
.brain/

# handoff-заметки между чатами (рабочий процесс, не код проекта; ссылаются на внутрянку/секрет-файлы)
# — как .brain, локальны, НЕ уезжают в публичный репо на «выкатывай»
handoffs/

# локальный тулинг Claude Code (launch.json превью с машинно-специфичным путём к питону) — не в общий репо
.claude/

# план-заметки агентского харнеса (zcode) — рабочий процесс, не код проекта; как .brain/handoffs
.zcode/

# сессии/токены
*.session

# окружение
venv/
node_modules/

# кэши тестов
.pytest_cache/
